const exitMining = (context, event) => { }
const exitFarming = (context, event) => { }
const exitBuilding = (context, event) => { }
const exitSleeping = (context, event) => { }
const exitFollowing = (context, event) => { }
const exitSheltering = (context, event) => { }

export default {
  exitBuilding,
  exitFarming,
  exitFollowing,
  exitMining,
  exitSleeping,
  exitSheltering
}