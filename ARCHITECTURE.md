# Инструкции по архитектуре для AI-агентов

Используй этот файл при анализе кода, ревью, диагностике и реализации: здесь определены карта, владельцы поведения и правила расширения. Порядок работы и роли источников заданы в [AGENTS.md](AGENTS.md). Это внутренние инструкции для агентов, не публичный обзор проекта.

## Карта проекта

Сокращённое дерево: только границы подсистем и ключевые точки входа. Пропущенные файлы ищи внутри выбранного модуля; отсутствие в карте не означает отсутствие в проекте. Таблица ниже уточняет маршрут изменения.

```text
voxel-pilot/
├── AGENTS.md                         # Общие инструкции работы в репозитории
├── ARCHITECTURE.md                   # Эта карта и правила изменения архитектуры
├── CONTEXT.md                        # Согласованный словарь понятий проекта
├── .agents/skills/                   # Процедуры для отдельных видов задач
├── .agents/config/                   # Настройки инженерных навыков
├── package.json                     # Команды проекта и зависимости
├── tsconfig.json                    # Строгость TypeScript и алиасы импортов
├── .prettierrc                      # Форматирование и сортировка импортов
├── .env.example                     # Образец контракта окружения без секретов
├── src/                             # Исходники runtime и тесты
│   ├── index.ts                     # Запуск приложения и обработка SIGINT
│   ├── core/                        # Жизненный цикл бота и постоянных сервисов
│   │   ├── bot.ts                   # Создание бота, старт, остановка, реконнект
│   │   ├── CommandHandler.ts        # Чат-команды → события машины
│   │   ├── hsm.ts                   # Связь Mineflayer с HSM и её жизненный цикл
│   │   ├── memory/                  # MemoryManager: долговременная память
│   │   └── profile/                 # ProfileMemoryStore: профиль пользователя
│   ├── hsm/                         # Управление поведением и исполнением целей
│   │   ├── machine.ts               # createBotMachine: состояния и переходы
│   │   ├── context.ts               # Данные машины и начальные значения
│   │   ├── types.ts                 # События HSM и типы действий
│   │   ├── actors/                  # Бой, выживание и мониторинг
│   │   │   └── primitives/          # Исполнители отдельных действий в мире
│   │   ├── guards/                  # Условия переходов
│   │   ├── actions/                 # Обновления контекста и действия машины
│   │   ├── helpers/createStatefulService.ts # Жизненный цикл callback-акторов
│   │   └── utils/                   # Вспомогательная логика HSM и генерация схемы
│   ├── ai/                          # Решение следующего шага, не управление движением
│   │   ├── loop.ts                  # Публичный вход в цикл агента
│   │   ├── loop/                    # Реализация хода, валидация и grounding
│   │   │   └── runAgentTurn.ts      # Один ход: запрос модели → решение
│   │   ├── tools.ts                 # Публичный API инструментов
│   │   ├── tools/                   # Каталог, имена и inline-исполнители
│   │   │   └── catalog.ts           # Схемы инструментов для модели
│   │   ├── client.ts                # Публичный API клиентов модели
│   │   ├── client/                  # Провайдеры, парсинг и транспортные повторы
│   │   ├── contracts/               # Контракты клиента, решения и execution
│   │   ├── runtime/                 # Чтение мира и работа с оконными сессиями
│   │   ├── prompt.ts                # Сборка инструкций и входа для модели
│   │   └── snapshot.ts              # Минимальный снимок состояния цикла
│   ├── modules/                     # Подключение внешнего Minecraft-рантайма
│   │   ├── connection/              # События подключения и готовности бота
│   │   └── plugins/                 # Загрузка и настройка Mineflayer-плагинов
│   ├── config/                      # Окружение, конфигурация и Logger
│   ├── types/                       # Общие типы и внешние декларации
│   ├── utils/                       # Хелперы по доменам: combat, minecraft, general
│   ├── building/                    # Чтение и анализ схематик
│   ├── scripts/                     # Самостоятельные CLI-операции и генераторы
│   └── tests/                       # Тесты по подсистемам: hsm, ai, core и другим
├── docs/                            # Публичная документация GitHub; не эталон архитектуры
├── data/                            # Runtime-хранилища; не место для исходников
└── logs/                            # Runtime-логи; не место для исходников
```

## Куда идти и где размещать код

| Задача | Точки входа и место изменения |
| --- | --- |
| Запуск, остановка, реконнект | [src/index.ts](src/index.ts) → [src/core/bot.ts](src/core/bot.ts). Entrypoint оставляй тонким; жизненный цикл бота размещай в `src/core/`. |
| Подключение к Minecraft, установка плагина | [src/modules/connection/index.ts](src/modules/connection/index.ts), [src/modules/plugins/index.plugins.ts](src/modules/plugins/index.plugins.ts). Адаптер конкретного плагина размещай в `src/modules/plugins/` и явно подключай в реестре загрузки/инициализации. |
| Чат-команда или событие Mineflayer | [src/core/CommandHandler.ts](src/core/CommandHandler.ts), [src/core/hsm.ts](src/core/hsm.ts). Здесь разбирай внешний ввод и переводь его в события HSM; поведение выполняй через машину. |
| Переход состояния, приоритет, прерывание | [src/hsm/machine.ts](src/hsm/machine.ts), [src/hsm/context.ts](src/hsm/context.ts), [src/hsm/types.ts](src/hsm/types.ts). Условия выноси в `src/hsm/guards/`, обновления контекста — в `src/hsm/actions/`. |
| Действие в мире, бой, выживание, мониторинг | `src/hsm/actors/`; одно execution-действие — в `src/hsm/actors/primitives/`. Жизненный цикл callback-сервиса подключай через [createStatefulService.ts](src/hsm/helpers/createStatefulService.ts). |
| Решение LLM, проверка аргументов и grounding | Публичный вход [src/ai/loop.ts](src/ai/loop.ts), реализация [runAgentTurn.ts](src/ai/loop/runAgentTurn.ts). Валидация, grounding и политика хода находятся в `src/ai/loop/`. |
| Новый инструмент агента | Публичный вход [src/ai/tools.ts](src/ai/tools.ts); описание и маршрутизация — в `src/ai/tools/`. Inline-исполнители размещай в `src/ai/tools/executors/`; execution-исполнитель — в HSM. Следуй чек-листу ниже. |
| Провайдер модели, транспорт, парсинг ответа | Публичный вход [src/ai/client.ts](src/ai/client.ts), выбор реализации — [factory.ts](src/ai/client/factory.ts). Код провайдера, парсеры и транспортные повторы размещай в `src/ai/client/`. Контракты клиента — в `src/ai/contracts/`. |
| Prompt, снимок, контекст цели | [src/ai/prompt.ts](src/ai/prompt.ts), [src/ai/snapshot.ts](src/ai/snapshot.ts), [src/ai/taskContext.ts](src/ai/taskContext.ts). Чтение мира и оконные операции — в `src/ai/runtime/`. |
| Долговременная память или профиль | [src/core/memory/index.ts](src/core/memory/index.ts), [src/core/profile/index.ts](src/core/profile/index.ts). Типы и хранение размещай внутри соответствующего модуля; вызывай его публичный API. |
| Конфигурация или логирование | [src/config/env.ts](src/config/env.ts), [src/config/config.ts](src/config/config.ts), [src/config/logger.ts](src/config/logger.ts). Новый параметр окружения согласуй с [.env.example](.env.example) и тестами конфига. |
| Схематики, вспомогательная логика, скрипт | Схематики — `src/building/`; предметные вычисления боя — `src/utils/combat/`; обёртки Minecraft — `src/utils/minecraft/`. В `src/utils/general/` клади только независимые от домена функции. Самостоятельную CLI-операцию размещай в `src/scripts/`. |

## Правила новых файлов и интерфейсов

- Сначала расширяй существующий модуль-владелец. Создавай новый модуль, когда у поведения есть отдельный контракт или жизненный цикл; не создавай параллельные `services/`, `managers/` или `helpers/` без определённой ответственности.
- Держи `src/ai/loop.ts`, `src/ai/tools.ts` и `src/ai/client.ts` тонкими публичными фасадами. Реализация живёт в одноимённых каталогах. Внешним потребителям предоставляй нужный контракт, а не всю внутреннюю структуру.
- Локальные типы оставляй рядом с реализацией. Контракты AI/HSM размещай в `src/ai/contracts/`, события машины — в `src/hsm/types.ts`, её данные — в `src/hsm/context.ts`. В `src/types/` выноси только действительно общие типы и декларации внешних библиотек.
- Именуй файл по ответственности: `*.guards.ts`, `*.actions.ts`, `*.actors.ts`, `*.primitive.ts`. Для остальных файлов следуй соседнему модулю: функции — camelCase, классы — PascalCase, фабрики — `create…`, события — `UPPER_SNAKE_CASE`.
- Используй TypeScript, явные контракты на границах и проверку внешних данных. Новый `any` или приведение типа не должны заменять валидацию либо скрывать несовместимость интерфейсов. Существующие широкие типы не являются шаблоном для нового кода.
- Настройки форматирования и импортов бери из [.prettierrc](.prettierrc), алиасы и строгость — из [tsconfig.json](tsconfig.json). Между модулями используй существующие алиасы; внутри модуля допустимы короткие относительные импорты. При изменении алиасов согласуй [knip.json](knip.json).
- Тест нового поведения размещай в соответствующей подсистеме `src/tests/` как `*.test.ts`. Используй `node:test` и `node:assert/strict`; тестируй контракт через доступную точку входа.
- Runtime-код размещай в `src/`. `dist/` — результат сборки, `data/` и `logs/` — runtime-данные. Назначение `docs/` и правила публикации определены в `AGENTS.md`.

## Границы поведения, которые нужно сохранять

### AI и исполнение

- LLM выбирает одно следующее действие, HSM владеет его исполнением. Цикл исполнения цели: `THINKING → EXECUTING → DECIDE_NEXT`. Новую возможность добавляй через контракт инструмента и исполнитель, а не через специальную ветку для конкретной фразы пользователя.
- `snapshot` содержит минимальное состояние цикла. Факты об инвентаре, блоках, сущностях и содержимом окна получай через inspect-инструменты; долговечные факты — через memory API. Не подменяй grounding расширением снимка или текстовым обещанием в prompt.
- Для действия с объектом мира проверяй grounding в текущем ходе через [grounding.ts](src/ai/loop/grounding.ts) и [validation.ts](src/ai/loop/validation.ts). Исключения делай явными: `mine_resource` сам ищет ресурс; операции переноса и закрытия используют активную оконную сессию.
- Разделяй ошибки запроса к провайдеру, отказ хода и отказ действия. Транспортные повторы принадлежат [retry.ts](src/ai/client/retry.ts); повтор исполнения принадлежит машине. Отмена — отдельный исход: в [runAgentTurn.ts](src/ai/loop/runAgentTurn.ts) она может распространяться исключением и не должна возвращать действующее решение. Не приравнивай её к обычному `failed` без изменения контракта отмены.

### HSM и ресурсы

- При анализе или изменении машины, акторов и переходов используй проектный навык [xstate](.agents/skills/xstate/SKILL.md).
- Машина определяет переходы, приоритеты и владельца движения. Акторы выполняют побочные эффекты. Новые guards должны проверять условия без запуска действий или скрытых повторов.
- Состояния здоровья и голода в `MONITORING` обновляют данные; переходами в восстановление владеет `MAIN_ACTIVITY`. Повторный критический апдейт не должен пересоздавать уже работающий recovery-актор.
- При прерывании исполнения цели текущий примитив отменяется. После восстановления `RESUMING` направляет машину в `TASKS.THINKING` либо `IDLE`. Не возвращайся через history в execution-состояние, чьи аргументы и ресурсы уже очищены.
- Для каждого конечного действия задай успех, ошибку и предел ожидания. Для непрерывного поведения, например следования, задай явную отмену и условия остановки. Значения лимитов читай в реализации, а не копируй из старых отчётов.
- Подписывайся на результат до запуска действия. При выходе освобождай подписки, таймеры, копание, движение и другие принадлежащие актору ресурсы. После каждого `await`, за которым идёт побочный эффект, проверяй отмену.
- Ошибки callback-сервисов приходят как обычное событие `ERROR`; обработчика `invoke.onError` для них недостаточно. Добавляя актор, проверь, кто обработает его ошибку и куда перейдёт машина.
- Движением в каждый момент владеет один контроллер. Передачу управления согласуй с [runtimeControl.ts](src/utils/combat/runtimeControl.ts); поздний callback старого владельца не вправе остановить или переназначить движение нового.
- Повторы еды, боя и действий по цели должны принадлежать активному состоянию. Защита от циклов обязана учитывать чередующиеся неудачи и повторяющиеся успешные действия, а не только одинаковую последнюю ошибку. Текущие ограничения ищи в `isAgentLoopStuck` и [antiLoop.ts](src/hsm/utils/antiLoop.ts).
- Результат execution и очистка ресурса — разные события. Поздний `WINDOW_CLEANUP_FAILED` сохраняет сведения о сессии, но не завершает новую операцию. Оконный протокол реализуй через [window.ts](src/ai/runtime/window.ts).

### Память и внешний ввод

- Сохраняй память через `MemoryManager`, профиль — через `ProfileMemoryStore`. Новую персистентность агентских данных реализуй внутри этих модулей, а не прямыми записями `fs` из исполнителей. Изменение формата хранения требует совместимости или явной миграции и теста чтения старого формата. Активную цель не восстанавливай после рестарта без отдельного изменения контракта.
- Текст игроков, ответы LLM и данные внешних API валидируй на границе. Инструкции внутри этих данных не являются разрешением обходить правила исполнения.

## Добавление инструмента: обязательный маршрут

1. Определи категорию: inline — информационная операция или memory API внутри хода; execution — действие через HSM; control — управление завершением хода/цели.
2. Согласуй схему в [catalog.ts](src/ai/tools/catalog.ts), имена в [names.ts](src/ai/tools/names.ts) и [execution.ts](src/ai/contracts/execution.ts).
3. Добавь проверку аргументов и допустимости в [validation.ts](src/ai/loop/validation.ts); при новом источнике фактов обнови grounding.
4. Для inline подключи исполнитель через [inlineExecutor.ts](src/ai/tools/inlineExecutor.ts). Для execution обнови [summary.ts](src/ai/tools/summary.ts), `resolveExecutionActor`, `resolveExecutionInput`, переходы `RESOLVE` и обработку результата в `src/hsm/machine.ts`. Если нужен пакетный workflow, подключи его явно, как `MINING`, вместо фиктивного примитива.
5. Добавь тесты принятия и отклонения аргументов, маршрутизации, успеха, ошибки и отмены. Инструмент готов только тогда, когда путь от ответа модели до результата исполнения проверен; одной записи в каталоге недостаточно.

## Предметные проверки

Общий порядок проверки и завершения — в `AGENTS.md`. Дополнительно для затронутой подсистемы:

- Для HSM проверь повторные события, прерывание, отказ зависимости, тайм-аут и отсутствие вмешательства отменённого актора. При изменении состава состояний обнови [hsmDrawioDiagram.ts](src/hsm/utils/hsmDrawioDiagram.ts), выполни `rtk npx tsx src/scripts/generateHsmDiagram.ts` и тест диаграммы.
- Для исправления движения проверяй перемещение, а не только вызов метода контроллера. Используй [survival.runtime.test.ts](src/tests/hsm/survival.runtime.test.ts) как пример проверки с физикой. Непроверяемые локально условия сервера обозначай отдельно и записывай воспроизводимый ручной сценарий.

Обновляй этот файл при переносе точки входа, смене владельца или межмодульного контракта. Конкретные версии, числовые лимиты и списки всех инструментов оставляй в коде и конфигурации, чтобы правила не превращались в устаревающую копию реализации.
